# LOATS13July2026 - Build-Wave Operating Rules for Cline (AUTO-LOADED EVERY TASK)

Source of authority: `23Aug2026-Seq ToDos.txt` (repo root) - REV-2, derived from `23Aug2026-Consolidated FR.md`.
These rules govern EVERY task in this workspace. They are the section-0 operating layer of the FR7 build wave. Do NOT edit this file; do NOT proceed past a checkpoint without user sign-off.

## Mission context (30 seconds)

This repo is a trading research system audited by a 7-review forensic chain. Four consecutive reviews documented commits claiming "FULLY COMPLIANT"/"READY FOR LIVE DEPLOYMENT" on a tree that FAILED its own CI. The current wave (TODO-1..27 in the Seq ToDos file) fixes that. Your job: execute TODOs strictly, verify with the health scripts, never fake green.

## Task protocol - every TODO, no exceptions

1. **Locate, don't load:** In `23Aug2026-Seq ToDos.txt`, SEARCH for the exact heading `TODO-n (` - read ONLY that cell (up to the next separator line of `&&&...` / `<=O0O=>`). NEVER read the whole file (89k chars - token waste). Section 0 is already inside this .clinerules; section 7 scripts already exist as files.
2. **Locate code by symbol/content, never by line number** - all line refs in the TODO file describe HEAD `163cdf9` and WILL have drifted after earlier TODOs.
3. **TDD where testable:** failing test FIRST for TODO-9, 10, 16, 17, 20; recommended elsewhere.
4. **Smallest correct change.** No drive-by refactors. No new lint ignores.
5. **Verify (the per-TODO gate protocol = Seq ToDos 7.4):**
   - `.\LOATS13July2026\Scripts\python.exe scripts\fr7_health_check.py --only <this TODO's HC ids>` - its check(s) MUST flip FAIL->PASS.
   - Then `.\LOATS13July2026\Scripts\python.exe scripts\fr7_health_check.py --fast` - exit 0 required: re-runs ALL quality gates (deps-sync, ruff check, ruff format, isort, flake8, mypy --strict, bandit) + DOMAIN-RULE checks (HC-14 OPS<=3, HC-15 source-gate math 3-reject/4-pass, HC-23 CMP config, HC-24 IV 30/40, HC-25 VIX fail-safe). Exit 1 = TODO not done.
   - Module tests: `.\LOATS13July2026\Scripts\python.exe -m pytest tests\test_<module>.py -q`.
   - FULL suite (`-m pytest tests\ -q`, ~2.5 min): MANDATORY after TODO-7, 8, 10, 12, 13, 14, 16, 17, 19, 20 (order/decision path) and at every checkpoint; recommended otherwise.
6. **Commit (one per TODO):** Conventional Commits, subject <=72 chars, body = evidence (HC ids + exit codes + test counts). NEVER claim compliance/verification not demonstrated by command output in THIS session. Zero tolerance.
7. **Completion record:** append under the TODO heading in the txt: `[DONE 2026-08-dd] HC-xx PASS - commit <short-hash>`.
8. TODO-22..27 have no inline Acceptance: the section-8 "Done when" matrix row is the criterion.

## Execution order (REV-2, authoritative)

TODO-1, 2, 18, 6, 5a, 7, 9, 8 [CP-2], 10, 11, 12, 16, 17, 13 [CP-3], 19 [CP-3], 14, 3, 15, 4, 5b [CP-4], 20, 21, 22-27. Supersedes any other ordering in the txt.

## User-approval checkpoints - STOP and ASK

- **CP-0** before TODO-1: wave go/no-go + confirm branch name `fix/fr7-wave`.
- **CP-1** after TODO-2+18+6: show HC-09 evidence (mypy green).
- **CP-2** before TODO-8: USER DECIDES Option A (4th volatility producer) vs Option B (diversity threshold 3/7 + ADR). Present effort/risk; do NOT pre-select.
- **CP-3** before TODO-13 AND before TODO-19: TODO-19 has Option A (retire legacy engine) vs B (convert to tagged producers) - user picks.
- **CP-4** wave end, before TODO-5b and wave-PR merge.

At a checkpoint: finish your turn with the decision question and WAIT. Between checkpoints: execute sequentially without prompting.

## Hard prohibitions (violation = stop and ask the user)

- Never weaken/skip an HC check, gate, exclusion, or CI step to force green (includes editing `fr7_health_check.py` check logic, `pyproject.toml` lint ignores, `.flake8`, `ci.yml`).
- Never delete/skip tests or lower coverage floors to pass gates.
- Never add blanket `type: ignore` / `# noqa`; each requires a written reason.
- Never re-introduce `PYTEST_CURRENT_TEST`-style environment sniffing.
- Never edit `.env`, commit secrets, or push directly to `main` once TODO-5a is active.
- Never fabricate success responses in any order/routing path (the F7-H-01 pathology).
- If a check is genuinely wrong: fix the CHECK, justify in the commit body.
- ANALYZE mode only on all order paths (CMP Rule 5). OpenAlgo REST is never called by HC probes.

## Environment facts

- Python = clean venv ONLY: `.\LOATS13July2026\Scripts\python.exe` (repo-root relative). System Python has 20+ conflicting packages - never use it.
- `scripts\fr7_health_check.py` + `scripts\fr7_health_snapshot.py` already exist (untracked, gate-clean). Baseline: `reports\health\health-00-baseline-20260823-201354.json` (8 PASS / 16 FAIL / 3 SKIP).
- Stage snapshots: `python scripts\fr7_health_snapshot.py --label stage-x` -> `reports\health\`.
- flake8 reads `.flake8` (NOT pyproject.toml); keep in sync with `[tool.ruff]` when either changes.
- External-env TODOs (4: pip-audit network; 25: live latency + 2-wk forward test; optional live HTTP in 13): implement + offline-test, hand user a runbook, mark DEFERRED in completion log.
- Wave branch: `fix/fr7-wave` (after TODO-5a, all work via PRs).
- Keep `23Aug2026-FR Sequential TODOs.md` (repo) and `G:\.OA\LOATS-13July2026\23Aug2026-Seq ToDos.txt` (master) in sync if either changes.
