# Runtime and development dependencies for LOATS13July2026.
# The canonical dependency declarations live in `pyproject.toml` and
# `requirements-core.txt`. This file previously held a machine-specific
# `pip freeze` dump (including external editable paths and the full local
# ML stack) that could not be installed on a fresh checkout.
#
# For a Docker/production-style install use:
#     pip install -r requirements-core.txt
#
# For a full project install (runtime + dev) use:
#     pip install -e ".[dev]"
#
-r requirements-core.txt
